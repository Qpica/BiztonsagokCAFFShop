import auth from './auth';
import features from './features';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [auth, features]
};

export default menuItems;
